name: Repair panel automatic slug

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  repair:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Manual repair only
        run: echo "Manual repair workflow. Automatic push trigger disabled for site stability."
