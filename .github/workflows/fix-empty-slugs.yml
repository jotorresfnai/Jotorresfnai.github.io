name: Fix panel slug generation and publish defaults

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  fix:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Repair panel manually
        run: echo "Manual repair workflow. Automatic push trigger disabled for site stability."
